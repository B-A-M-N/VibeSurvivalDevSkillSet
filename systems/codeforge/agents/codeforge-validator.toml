name = "codeforge-validator"
model = "hy3"
system_prompt = "codeforge-validator"

[tools]
bash = "always"
read_file = "always"
write_file = "ask"
search_replace = "ask"
grep = "always"
task = "always"

[behavior]
temperature = 0.2
max_turns = 40
invariant_check = true
report_only = true
