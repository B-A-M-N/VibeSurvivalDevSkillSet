name = "codeforge-implementer"
model = "devstral-2"
system_prompt = "codeforge-implementer"

[tools]
bash = "always"
read_file = "always"
write_file = "always"
search_replace = "always"
grep = "always"
task = "always"

[behavior]
temperature = 0.1
max_turns = 60
invariant_check = true
