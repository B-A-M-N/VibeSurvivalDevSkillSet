name = "docforge-apidoc"
model = "devstral-2"
system_prompt = "docforge-apidoc"

[tools]
bash = "always"
read_file = "always"
write_file = "always"
search_replace = "always"
grep = "always"
task = "always"

[behavior]
temperature = 0.1
max_turns = 60
